#!/usr/bin/env bash
# This script was generated by bashly 0.7.10 (https://bashly.dannyb.co)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if [[ "${BASH_VERSINFO:-0}" -lt 4 ]]; then
  printf "bash version 4 or higher is required\n"
  exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
  echo "$version"
}

# :command.usage
zen_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen - Zen is a workflow management tool.\n"
    echo

  else
    printf "zen - Zen is a workflow management tool.\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen [command]\n"
  printf "  zen [command] --help | -h\n"
  printf "  zen --version | -v\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  todo    Interact with todos"
  echo "  card    Interact with cards"
  echo "  flow    Interact with workflows"
  echo "  pr      Interact with pull requests"
  echo "  local   Interact with local dev environment"
  echo "  repo    Interact with local repo"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo
    echo "  --version, -v"
    printf "    Show version number\n"
    echo

  fi
}

# :command.usage
zen_todo_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo - Interact with todos\n"
    echo

  else
    printf "zen todo - Interact with todos\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo [command]\n"
  printf "  zen todo [command] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  list       List all todos"
  echo "  next       Show next todo"
  echo "  start      Start the next todo"
  echo "  current    Show the current todo"
  echo "  pause      Pause the current todo"
  echo "  complete   Complete the current todo"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_todo_list_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo list - List all todos\n"
    echo

  else
    printf "zen todo list - List all todos\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo list\n"
  printf "  zen todo list --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_todo_next_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo next - Show next todo\n"
    echo

  else
    printf "zen todo next - Show next todo\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo next [LIMIT]\n"
  printf "  zen todo next --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "Arguments:\n"

    # :argument.usage
    echo "  LIMIT"
    printf "    Limit for next todo\n"
    printf "    Default: 1\n"
    echo

  fi
}

# :command.usage
zen_todo_start_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo start - Start the next todo\n"
    echo

  else
    printf "zen todo start - Start the next todo\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo start\n"
  printf "  zen todo start --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_todo_current_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo current - Show the current todo\n"
    echo

  else
    printf "zen todo current - Show the current todo\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo current\n"
  printf "  zen todo current --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_todo_pause_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo pause - Pause the current todo\n"
    echo

  else
    printf "zen todo pause - Pause the current todo\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo pause\n"
  printf "  zen todo pause --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_todo_complete_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo complete - Complete the current todo\n"
    echo

  else
    printf "zen todo complete - Complete the current todo\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo complete\n"
  printf "  zen todo complete --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_card_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen card - Interact with cards\n"
    echo

  else
    printf "zen card - Interact with cards\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen card [command]\n"
  printf "  zen card [command] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  list   List all assigned cards"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_card_list_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen card list - List all assigned cards\n"
    echo

  else
    printf "zen card list - List all assigned cards\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen card list\n"
  printf "  zen card list --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_flow_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen flow - Interact with workflows\n"
    echo

  else
    printf "zen flow - Interact with workflows\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen flow [command]\n"
  printf "  zen flow [command] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  apply   Apply local changes to remote"
  echo "  amend   Amend the last changeset message"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_flow_apply_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen flow apply - Apply local changes to remote\n"
    echo

  else
    printf "zen flow apply - Apply local changes to remote\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen flow apply\n"
  printf "  zen flow apply --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_flow_amend_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen flow amend - Amend the last changeset message\n"
    echo

  else
    printf "zen flow amend - Amend the last changeset message\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen flow amend MESSAGE\n"
  printf "  zen flow amend --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "Arguments:\n"

    # :argument.usage
    echo "  MESSAGE"
    printf "    Amended message\n"
    echo

  fi
}

# :command.usage
zen_pr_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen pr - Interact with pull requests\n"
    echo

  else
    printf "zen pr - Interact with pull requests\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen pr [command]\n"
  printf "  zen pr [command] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  list   List all pull requests"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_pr_list_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen pr list - List all pull requests\n"
    echo

  else
    printf "zen pr list - List all pull requests\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen pr list\n"
  printf "  zen pr list --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local - Interact with local dev environment\n"
    echo

  else
    printf "zen local - Interact with local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local [command]\n"
  printf "  zen local [command] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  build     Build the local dev environment"
  echo "  clean     Clean the local dev environment"
  echo "  start     Start the local dev environment"
  echo "  stop      Stop the local dev environment"
  echo "  restart   Restart the local dev environment"
  echo "  shell     Start a tty shell in the local dev environment"
  echo "  console   Start a rails console in the local dev environment"
  echo "  rspec     Run rspec tests in the local test environment"
  echo "  cuke      Run cucumber tests"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_build_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local build - Build the local dev environment\n"
    echo

  else
    printf "zen local build - Build the local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local build\n"
  printf "  zen local build --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_clean_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local clean - Clean the local dev environment\n"
    echo

  else
    printf "zen local clean - Clean the local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local clean\n"
  printf "  zen local clean --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_start_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local start - Start the local dev environment\n"
    echo

  else
    printf "zen local start - Start the local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local start\n"
  printf "  zen local start --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_stop_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local stop - Stop the local dev environment\n"
    echo

  else
    printf "zen local stop - Stop the local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local stop\n"
  printf "  zen local stop --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_restart_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local restart - Restart the local dev environment\n"
    echo

  else
    printf "zen local restart - Restart the local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local restart\n"
  printf "  zen local restart --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_shell_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local shell - Start a tty shell in the local dev environment\n"
    echo

  else
    printf "zen local shell - Start a tty shell in the local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local shell\n"
  printf "  zen local shell --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_console_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local console - Start a rails console in the local dev environment\n"
    echo

  else
    printf "zen local console - Start a rails console in the local dev environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local console\n"
  printf "  zen local console --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_local_rspec_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local rspec - Run rspec tests in the local test environment\n"
    echo

  else
    printf "zen local rspec - Run rspec tests in the local test environment\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local rspec [SPEC_PATH]\n"
  printf "  zen local rspec --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "Arguments:\n"

    # :argument.usage
    echo "  SPEC_PATH"
    printf "    Path to spec\n"
    printf "    Default: \n"
    echo

  fi
}

# :command.usage
zen_local_cuke_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen local cuke - Run cucumber tests\n"
    echo

  else
    printf "zen local cuke - Run cucumber tests\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen local cuke [FEATURE_PATH]\n"
  printf "  zen local cuke --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "Arguments:\n"

    # :argument.usage
    echo "  FEATURE_PATH"
    printf "    Path to feature tests\n"
    printf "    Default: \n"
    echo

  fi
}

# :command.usage
zen_repo_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen repo - Interact with local repo\n"
    echo

  else
    printf "zen repo - Interact with local repo\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen repo [command]\n"
  printf "  zen repo [command] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  branch    Create a new branch in the repo"
  echo "  stage     Stage local changes"
  echo "  unstage   Unstage local changes"
  echo "  checkin   Checkin changes in the branch"
  echo "  update    Update changes in the branch"
  echo "  publish   Publish changes to the remote"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_repo_branch_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen repo branch - Create a new branch in the repo\n"
    echo

  else
    printf "zen repo branch - Create a new branch in the repo\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen repo branch NAME\n"
  printf "  zen repo branch --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "Arguments:\n"

    # :argument.usage
    echo "  NAME"
    printf "    Name of branch to create\n"
    echo

  fi
}

# :command.usage
zen_repo_stage_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen repo stage - Stage local changes\n"
    echo

  else
    printf "zen repo stage - Stage local changes\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen repo stage\n"
  printf "  zen repo stage --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_repo_unstage_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen repo unstage - Unstage local changes\n"
    echo

  else
    printf "zen repo unstage - Unstage local changes\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen repo unstage [options]\n"
  printf "  zen repo unstage --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo
    # :command.usage_flags
    # :flag.usage
    echo "  --nuke"
    printf "    Nuke local changes\n"
    echo

  fi
}

# :command.usage
zen_repo_checkin_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen repo checkin - Checkin changes in the branch\n"
    echo

  else
    printf "zen repo checkin - Checkin changes in the branch\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen repo checkin\n"
  printf "  zen repo checkin --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_repo_update_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen repo update - Update changes in the branch\n"
    echo

  else
    printf "zen repo update - Update changes in the branch\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen repo update\n"
  printf "  zen repo update --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_repo_publish_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen repo publish - Publish changes to the remote\n"
    echo

  else
    printf "zen repo publish - Publish changes to the remote\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen repo publish\n"
  printf "  zen repo publish --help | -h\n"
  echo

  if [[ -n $long_usage ]]; then
    printf "Options:\n"
    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.normalize_input
normalize_input() {
  local arg flags

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for (( i=0 ; i < ${#flags} ; i++ )); do
        input+=("-${flags:i:1}")
      done
    else
      input+=("$arg")
    fi

    shift
  done
}
# :command.inspect_args
inspect_args() {
  readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
  if (( ${#args[@]} )); then
    echo args:
    for k in "${sorted_keys[@]}"; do echo "- \${args[$k]} = ${args[$k]}"; done
  else
    echo args: none
  fi

  if (( ${#other_args[@]} )); then
    echo
    echo other_args:
    echo "- \${other_args[*]} = ${other_args[*]}"
    for i in "${!other_args[@]}"; do
      echo "- \${other_args[$i]} = ${other_args[$i]}"
    done
  fi
}

# :command.command_functions

# :command.function
zen_todo_list_command() {
  # src/todo_list_command.sh
  # echo "# this file is located in 'src/todo_list_command.sh'"
  # echo "# code for 'zen todo list' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args
  todo.sh ls
}

# :command.function
zen_todo_next_command() {
  # src/todo_next_command.sh
  # echo "# this file is located in 'src/todo_next_command.sh'"
  # echo "# code for 'zen todo next' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args
  todo.sh ls | head -n ${args[limit]}
}

# :command.function
zen_todo_start_command() {
  # src/todo_start_command.sh
  echo "# this file is located in 'src/todo_start_command.sh'"
  echo "# code for 'zen todo start' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_todo_current_command() {
  # src/todo_current_command.sh
  echo "# this file is located in 'src/todo_current_command.sh'"
  echo "# code for 'zen todo current' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_todo_pause_command() {
  # src/todo_pause_command.sh
  echo "# this file is located in 'src/todo_pause_command.sh'"
  echo "# code for 'zen todo pause' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_todo_complete_command() {
  # src/todo_complete_command.sh
  echo "# this file is located in 'src/todo_complete_command.sh'"
  echo "# code for 'zen todo complete' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_card_list_command() {
  # src/card_list_command.sh
  # echo "# this file is located in 'src/card_list_command.sh'"
  # echo "# code for 'zen card list' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args
  ${MY_CODE}/myjira/jira issues assigned
}

# :command.function
zen_flow_apply_command() {
  # src/flow_apply_command.sh
  # zen repo update
  zen repo stage
  zen repo checkin
  zen repo publish
}

# :command.function
zen_flow_amend_command() {
  # src/flow_amend_command.sh
  git commit -amend "${args[message]}"
}

# :command.function
zen_pr_list_command() {
  # src/pr_list_command.sh
  # echo "# this file is located in 'src/pr_list_command.sh'"
  # echo "# code for 'zen pr list' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args
  gh pr list
}

# :command.function
zen_local_build_command() {
  # src/local_build_command.sh
  cwd = $PWD

  ${cwd}/bin/run build ${args}
}

# :command.function
zen_local_clean_command() {
  # src/local_clean_command.sh
  cwd = $PWD

  ${cwd}/bin/run clean
}

# :command.function
zen_local_start_command() {
  # src/local_start_command.sh
  cwd = $PWD

  ${cwd}/bin/run start
}

# :command.function
zen_local_stop_command() {
  # src/local_stop_command.sh
  cwd = $PWD

  ${cwd}/bin/run stop
}

# :command.function
zen_local_restart_command() {
  # src/local_restart_command.sh
  cwd = $PWD

  ${cwd}/bin/run restart
}

# :command.function
zen_local_shell_command() {
  # src/local_shell_command.sh
  echo "# this file is located in 'src/local_shell_command.sh'"
  echo "# code for 'zen local shell' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_local_console_command() {
  # src/local_console_command.sh
  echo "# this file is located in 'src/local_console_command.sh'"
  echo "# code for 'zen local console' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_local_rspec_command() {
  # src/local_rspec_command.sh
  echo "# this file is located in 'src/local_rspec_command.sh'"
  echo "# code for 'zen local rspec' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_local_cuke_command() {
  # src/local_cuke_command.sh
  echo "# this file is located in 'src/local_cuke_command.sh'"
  echo "# code for 'zen local cuke' goes here"
  echo "# you can edit it freely and regenerate (it will not be overwritten)"
  inspect_args
}

# :command.function
zen_repo_branch_command() {
  # src/repo_branch_command.sh
  git checkout -b ${args[name]}
  git push -u origin HEAD
}

# :command.function
zen_repo_stage_command() {
  # src/repo_stage_command.sh
  git add .
  git diff --cached
}

# :command.function
zen_repo_unstage_command() {
  # src/repo_unstage_command.sh
  if [[ ${args[--nuke]} -eq 1 ]]; then
    git reset HEAD --hard
  else
    git reset HEAD
  fi
}

# :command.function
zen_repo_checkin_command() {
  # src/repo_checkin_command.sh
  git commit
}

# :command.function
zen_repo_update_command() {
  # src/repo_update_command.sh
  git pull --rebase origin HEAD
}

# :command.function
zen_repo_publish_command() {
  # src/repo_publish_command.sh
  git push -u origin HEAD
}

# :command.parse_requirements
parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --version | -v )
    version_command
    exit
    ;;

  --help | -h )
    long_usage=yes
    zen_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  todo )
    action="todo"
    shift
    zen_todo_parse_requirements "$@"
    shift $#
    ;;

  card )
    action="card"
    shift
    zen_card_parse_requirements "$@"
    shift $#
    ;;

  flow )
    action="flow"
    shift
    zen_flow_parse_requirements "$@"
    shift $#
    ;;

  pr )
    action="pr"
    shift
    zen_pr_parse_requirements "$@"
    shift $#
    ;;

  local )
    action="local"
    shift
    zen_local_parse_requirements "$@"
    shift $#
    ;;

  repo )
    action="repo"
    shift
    zen_repo_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  * )
    zen_usage
    exit 1
    ;;

  esac
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_todo_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  list )
    action="list"
    shift
    zen_todo_list_parse_requirements "$@"
    shift $#
    ;;

  next )
    action="next"
    shift
    zen_todo_next_parse_requirements "$@"
    shift $#
    ;;

  start )
    action="start"
    shift
    zen_todo_start_parse_requirements "$@"
    shift $#
    ;;

  current )
    action="current"
    shift
    zen_todo_current_parse_requirements "$@"
    shift $#
    ;;

  pause )
    action="pause"
    shift
    zen_todo_pause_parse_requirements "$@"
    shift $#
    ;;

  complete )
    action="complete"
    shift
    zen_todo_complete_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  * )
    zen_todo_usage
    exit 1
    ;;

  esac
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_todo_list_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_list_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="todo list"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_todo_next_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_next_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="todo next"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      if [[ -z ${args[limit]+x} ]]; then
        # :argument.validations
        args[limit]=$1
        shift
      else
        printf "invalid argument: %s\n" "$key"
        exit 1
      fi
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  [[ -n ${args[limit]:-} ]] || args[limit]="1"
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_todo_start_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_start_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="todo start"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_todo_current_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_current_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="todo current"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_todo_pause_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_pause_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="todo pause"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_todo_complete_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_complete_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="todo complete"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_card_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_card_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  list )
    action="list"
    shift
    zen_card_list_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  * )
    zen_card_usage
    exit 1
    ;;

  esac
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_card_list_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_card_list_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="card list"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_flow_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_flow_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  apply )
    action="apply"
    shift
    zen_flow_apply_parse_requirements "$@"
    shift $#
    ;;

  amend )
    action="amend"
    shift
    zen_flow_amend_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  * )
    zen_flow_usage
    exit 1
    ;;

  esac
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_flow_apply_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_flow_apply_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="flow apply"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_flow_amend_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_flow_amend_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="flow amend"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      if [[ -z ${args[message]+x} ]]; then
        # :argument.validations
        args[message]=$1
        shift
      else
        printf "invalid argument: %s\n" "$key"
        exit 1
      fi
      ;;

    esac
  done
  # :command.required_args_filter
  if [[ -z ${args[message]+x} ]]; then
    printf "missing required argument: MESSAGE\nusage: zen flow amend MESSAGE\n"
    exit 1
  fi
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_pr_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_pr_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  list )
    action="list"
    shift
    zen_pr_list_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  * )
    zen_pr_usage
    exit 1
    ;;

  esac
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_pr_list_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_pr_list_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="pr list"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  build )
    action="build"
    shift
    zen_local_build_parse_requirements "$@"
    shift $#
    ;;

  clean )
    action="clean"
    shift
    zen_local_clean_parse_requirements "$@"
    shift $#
    ;;

  start )
    action="start"
    shift
    zen_local_start_parse_requirements "$@"
    shift $#
    ;;

  stop )
    action="stop"
    shift
    zen_local_stop_parse_requirements "$@"
    shift $#
    ;;

  restart )
    action="restart"
    shift
    zen_local_restart_parse_requirements "$@"
    shift $#
    ;;

  shell )
    action="shell"
    shift
    zen_local_shell_parse_requirements "$@"
    shift $#
    ;;

  console )
    action="console"
    shift
    zen_local_console_parse_requirements "$@"
    shift $#
    ;;

  rspec )
    action="rspec"
    shift
    zen_local_rspec_parse_requirements "$@"
    shift $#
    ;;

  cuke )
    action="cuke"
    shift
    zen_local_cuke_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  * )
    zen_local_usage
    exit 1
    ;;

  esac
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_build_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_build_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local build"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_clean_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_clean_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local clean"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_start_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_start_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local start"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_stop_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_stop_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local stop"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_restart_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_restart_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local restart"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_shell_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_shell_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local shell"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_console_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_console_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local console"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_rspec_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_rspec_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local rspec"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      if [[ -z ${args[spec_path]+x} ]]; then
        # :argument.validations
        args[spec_path]=$1
        shift
      else
        printf "invalid argument: %s\n" "$key"
        exit 1
      fi
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  [[ -n ${args[spec_path]:-} ]] || args[spec_path]=""
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_local_cuke_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_local_cuke_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="local cuke"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      if [[ -z ${args[feature_path]+x} ]]; then
        # :argument.validations
        args[feature_path]=$1
        shift
      else
        printf "invalid argument: %s\n" "$key"
        exit 1
      fi
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  [[ -n ${args[feature_path]:-} ]] || args[feature_path]=""
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_repo_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_repo_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  branch )
    action="branch"
    shift
    zen_repo_branch_parse_requirements "$@"
    shift $#
    ;;

  stage )
    action="stage"
    shift
    zen_repo_stage_parse_requirements "$@"
    shift $#
    ;;

  unstage )
    action="unstage"
    shift
    zen_repo_unstage_parse_requirements "$@"
    shift $#
    ;;

  checkin )
    action="checkin"
    shift
    zen_repo_checkin_parse_requirements "$@"
    shift $#
    ;;

  update )
    action="update"
    shift
    zen_repo_update_parse_requirements "$@"
    shift $#
    ;;

  publish )
    action="publish"
    shift
    zen_repo_publish_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  * )
    zen_repo_usage
    exit 1
    ;;

  esac
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_repo_branch_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_repo_branch_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="repo branch"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      if [[ -z ${args[name]+x} ]]; then
        # :argument.validations
        args[name]=$1
        shift
      else
        printf "invalid argument: %s\n" "$key"
        exit 1
      fi
      ;;

    esac
  done
  # :command.required_args_filter
  if [[ -z ${args[name]+x} ]]; then
    printf "missing required argument: NAME\nusage: zen repo branch NAME\n"
    exit 1
  fi
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_repo_stage_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_repo_stage_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="repo stage"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_repo_unstage_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_repo_unstage_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="repo unstage"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
    # :flag.case
    --nuke )
      # :flag.conflicts
      # :flag.case_no_arg
      args[--nuke]=1
      shift
      ;;

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_repo_checkin_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_repo_checkin_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="repo checkin"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_repo_update_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_repo_update_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="repo update"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.parse_requirements
zen_repo_publish_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_repo_publish_usage
    exit
    ;;

  esac
  # :command.environment_variables_filter
  # :command.dependencies_filter
  # :command.command_filter
  action="repo publish"
  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key"
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key"
      exit 1
      ;;

    esac
  done
  # :command.required_args_filter
  # :command.required_flags_filter
  # :command.catch_all_filter
  # :command.default_assignments
  # :command.whitelist_filter
  # :command.user_filter
}

# :command.initialize
initialize() {
  version="0.1.0"
  long_usage=''
  set -e

  # src/initialize.sh
}

# :command.run
run() {
  declare -A args=()
  declare -a other_args=()
  declare -a input=()
  normalize_input "$@"
  parse_requirements "${input[@]}"

  if [[ $action == "todo" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_usage
    else
      zen_todo_command
    fi

  elif [[ $action == "todo list" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_list_usage
    else
      zen_todo_list_command
    fi

  elif [[ $action == "todo next" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_next_usage
    else
      zen_todo_next_command
    fi

  elif [[ $action == "todo start" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_start_usage
    else
      zen_todo_start_command
    fi

  elif [[ $action == "todo current" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_current_usage
    else
      zen_todo_current_command
    fi

  elif [[ $action == "todo pause" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_pause_usage
    else
      zen_todo_pause_command
    fi

  elif [[ $action == "todo complete" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_complete_usage
    else
      zen_todo_complete_command
    fi

  elif [[ $action == "card" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_card_usage
    else
      zen_card_command
    fi

  elif [[ $action == "card list" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_card_list_usage
    else
      zen_card_list_command
    fi

  elif [[ $action == "flow" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_flow_usage
    else
      zen_flow_command
    fi

  elif [[ $action == "flow apply" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_flow_apply_usage
    else
      zen_flow_apply_command
    fi

  elif [[ $action == "flow amend" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_flow_amend_usage
    else
      zen_flow_amend_command
    fi

  elif [[ $action == "pr" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_pr_usage
    else
      zen_pr_command
    fi

  elif [[ $action == "pr list" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_pr_list_usage
    else
      zen_pr_list_command
    fi

  elif [[ $action == "local" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_usage
    else
      zen_local_command
    fi

  elif [[ $action == "local build" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_build_usage
    else
      zen_local_build_command
    fi

  elif [[ $action == "local clean" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_clean_usage
    else
      zen_local_clean_command
    fi

  elif [[ $action == "local start" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_start_usage
    else
      zen_local_start_command
    fi

  elif [[ $action == "local stop" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_stop_usage
    else
      zen_local_stop_command
    fi

  elif [[ $action == "local restart" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_restart_usage
    else
      zen_local_restart_command
    fi

  elif [[ $action == "local shell" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_shell_usage
    else
      zen_local_shell_command
    fi

  elif [[ $action == "local console" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_console_usage
    else
      zen_local_console_command
    fi

  elif [[ $action == "local rspec" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_rspec_usage
    else
      zen_local_rspec_command
    fi

  elif [[ $action == "local cuke" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_local_cuke_usage
    else
      zen_local_cuke_command
    fi

  elif [[ $action == "repo" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_repo_usage
    else
      zen_repo_command
    fi

  elif [[ $action == "repo branch" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_repo_branch_usage
    else
      zen_repo_branch_command
    fi

  elif [[ $action == "repo stage" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_repo_stage_usage
    else
      zen_repo_stage_command
    fi

  elif [[ $action == "repo unstage" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_repo_unstage_usage
    else
      zen_repo_unstage_command
    fi

  elif [[ $action == "repo checkin" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_repo_checkin_usage
    else
      zen_repo_checkin_command
    fi

  elif [[ $action == "repo update" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_repo_update_usage
    else
      zen_repo_update_command
    fi

  elif [[ $action == "repo publish" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_repo_publish_usage
    else
      zen_repo_publish_command
    fi

  elif [[ $action == "root" ]]; then
    root_command
  fi
}

initialize
run "$@"
