#!/usr/bin/env bash
# This script was generated by bashly 0.8.10 (https://bashly.dannyb.co)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if [[ "${BASH_VERSINFO:-0}" -lt 4 ]]; then
  printf "bash version 4 or higher is required\n" >&2
  exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
  echo "$version"
}

# :command.usage
zen_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen - Zen is a workflow management tool.\n"
    echo

  else
    printf "zen - Zen is a workflow management tool.\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen COMMAND\n"
  printf "  zen [COMMAND] --help | -h\n"
  printf "  zen --version | -v\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  note   Interact with notes"
  echo "  todo   Interact with todos"
  echo "  spec   Interact with specs"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo
    echo "  --version, -v"
    printf "    Show version number\n"
    echo

  fi
}

# :command.usage
zen_note_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen note - Interact with notes\n"
    echo

  else
    printf "zen note - Interact with notes\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen note COMMAND\n"
  printf "  zen note [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  list   List all notes"
  echo "  add    Add a note"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_environment_variables
    printf "Environment Variables:\n"

    # :environment_variable.usage
    echo "  NOTES_FILE"
    printf "    Notes file location\n"
    printf "    Default: $HOME/notes.txt\n"
    echo

  fi
}

# :command.usage
zen_note_list_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen note list - List all notes\n"
    echo

  else
    printf "zen note list - List all notes\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen note list\n"
  printf "  zen note list --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_note_add_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen note add - Add a note\n"
    echo

  else
    printf "zen note add - Add a note\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen note add BODY [OPTIONS]\n"
  printf "  zen note add --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_flags
    # :flag.usage
    echo "  --label, -l LABEL"
    printf "    Note label\n"
    printf "    Default: NOTE\n"
    echo

    # :command.usage_args
    printf "Arguments:\n"

    # :argument.usage
    echo "  BODY"
    printf "    Text for the note\n"
    echo

  fi
}

# :command.usage
zen_todo_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo - Interact with todos\n"
    echo

  else
    printf "zen todo - Interact with todos\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo COMMAND\n"
  printf "  zen todo [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  list   List all todos"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_environment_variables
    printf "Environment Variables:\n"

    # :environment_variable.usage
    echo "  TODOS_FILE"
    printf "    Todo file location\n"
    printf "    Default: $HOME/todo.txt\n"
    echo

  fi
}

# :command.usage
zen_todo_list_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen todo list - List all todos\n"
    echo

  else
    printf "zen todo list - List all todos\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen todo list\n"
  printf "  zen todo list --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_spec_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen spec - Interact with specs\n"
    echo

  else
    printf "zen spec - Interact with specs\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen spec COMMAND\n"
  printf "  zen spec [COMMAND] --help | -h\n"
  echo
  # :command.usage_commands
  printf "Commands:\n"
  echo "  describe   Describe spec"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.usage
zen_spec_describe_usage() {
  if [[ -n $long_usage ]]; then
    printf "zen spec describe - Describe spec\n"
    echo

  else
    printf "zen spec describe - Describe spec\n"
    echo

  fi

  printf "Usage:\n"
  printf "  zen spec describe FILE\n"
  printf "  zen spec describe --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n $long_usage ]]; then
    printf "Options:\n"

    # :command.usage_fixed_flags
    echo "  --help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "Arguments:\n"

    # :argument.usage
    echo "  FILE"
    printf "    Filename for the spec\n"
    echo

  fi
}

# :command.normalize_input
normalize_input() {
  local arg flags

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for (( i=0 ; i < ${#flags} ; i++ )); do
        input+=("-${flags:i:1}")
      done
    else
      input+=("$arg")
    fi

    shift
  done
}
# :command.inspect_args
inspect_args() {
  readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
  if (( ${#args[@]} )); then
    echo args:
    for k in "${sorted_keys[@]}"; do echo "- \${args[$k]} = ${args[$k]}"; done
  else
    echo args: none
  fi

  if (( ${#other_args[@]} )); then
    echo
    echo other_args:
    echo "- \${other_args[*]} = ${other_args[*]}"
    for i in "${!other_args[@]}"; do
      echo "- \${other_args[$i]} = ${other_args[$i]}"
    done
  fi
}

# :command.command_functions

# :command.function
zen_note_list_command() {
  # src/note_list_command.sh
  cat ${NOTES_FILE:-}
}

# :command.function
zen_note_add_command() {
  # src/note_add_command.sh
  now="$(date "+%Y%m%dT%H%M%S")"
  echo "${now} ${args[--label]}": >> ${NOTES_FILE:-}
  echo "${args[body]}" >> ${NOTES_FILE:-}
}

# :command.function
zen_todo_list_command() {
  # src/todo_list_command.sh
  # echo "# this file is located in 'src/todo_list_command.sh'"
  # echo "# code for 'zen todo list' goes here"
  # echo "# you can edit it freely and regenerate (it will not be overwritten)"
  # inspect_args
  todo.sh ls
}

# :command.function
zen_spec_describe_command() {
  # src/spec_describe_command.sh
  rg "describe|context|it" ${args[file]}

}

# :command.parse_requirements
parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --version | -v )
    version_command
    exit
    ;;

  --help | -h )
    long_usage=yes
    zen_usage
    exit
    ;;

  esac

  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  note )
    action="note"
    shift
    zen_note_parse_requirements "$@"
    shift $#
    ;;

  todo )
    action="todo"
    shift
    zen_todo_parse_requirements "$@"
    shift $#
    ;;

  spec )
    action="spec"
    shift
    zen_spec_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  "" )
    zen_usage >&2
    exit 1
    ;;

  * )
    printf "invalid command: %s\n" "$action" >&2
    exit 1
    ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key" >&2
      exit 1

      ;;

    esac
  done

}

# :command.parse_requirements
zen_note_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_note_usage
    exit
    ;;

  esac

  # :command.environment_variables_filter
  # :command.environment_variables_default
  export NOTES_FILE="${NOTES_FILE:-$HOME/notes.txt}"

  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  list )
    action="list"
    shift
    zen_note_list_parse_requirements "$@"
    shift $#
    ;;

  add )
    action="add"
    shift
    zen_note_add_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  "" )
    zen_note_usage >&2
    exit 1
    ;;

  * )
    printf "invalid command: %s\n" "$action" >&2
    exit 1
    ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key" >&2
      exit 1

      ;;

    esac
  done

}

# :command.parse_requirements
zen_note_list_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_note_list_usage
    exit
    ;;

  esac

  # :command.command_filter
  action="note list"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key" >&2
      exit 1

      ;;

    esac
  done

}

# :command.parse_requirements
zen_note_add_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_note_add_usage
    exit
    ;;

  esac

  # :command.command_filter
  action="note add"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
    # :flag.case
    --label | -l )

      # :flag.case_arg
      if [[ -n ${2+x} ]]; then

        args[--label]="$2"
        shift
        shift
      else
        printf "%s\n" "--label requires an argument: --label, -l LABEL" >&2
        exit 1
      fi
      ;;

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      if [[ -z ${args[body]+x} ]]; then

        args[body]=$1
        shift
      else
        printf "invalid argument: %s\n" "$key" >&2
        exit 1
      fi

      ;;

    esac
  done

  # :command.required_args_filter
  if [[ -z ${args[body]+x} ]]; then
    printf "missing required argument: BODY\nusage: zen note add BODY [OPTIONS]\n" >&2
    exit 1
  fi

  # :command.default_assignments
  [[ -n ${args[--label]:-} ]] || args[--label]="NOTE"

}

# :command.parse_requirements
zen_todo_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_usage
    exit
    ;;

  esac

  # :command.environment_variables_filter
  # :command.environment_variables_default
  export TODOS_FILE="${TODOS_FILE:-$HOME/todo.txt}"

  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  list )
    action="list"
    shift
    zen_todo_list_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  "" )
    zen_todo_usage >&2
    exit 1
    ;;

  * )
    printf "invalid command: %s\n" "$action" >&2
    exit 1
    ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key" >&2
      exit 1

      ;;

    esac
  done

}

# :command.parse_requirements
zen_todo_list_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_todo_list_usage
    exit
    ;;

  esac

  # :command.command_filter
  action="todo list"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key" >&2
      exit 1

      ;;

    esac
  done

}

# :command.parse_requirements
zen_spec_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_spec_usage
    exit
    ;;

  esac

  # :command.command_filter
  action=${1:-}

  case $action in
  -* )
    ;;

  describe )
    action="describe"
    shift
    zen_spec_describe_parse_requirements "$@"
    shift $#
    ;;

  # :command.command_fallback
  "" )
    zen_spec_usage >&2
    exit 1
    ;;

  * )
    printf "invalid command: %s\n" "$action" >&2
    exit 1
    ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      printf "invalid argument: %s\n" "$key" >&2
      exit 1

      ;;

    esac
  done

}

# :command.parse_requirements
zen_spec_describe_parse_requirements() {
  # :command.fixed_flags_filter
  case "${1:-}" in
  --help | -h )
    long_usage=yes
    zen_spec_describe_usage
    exit
    ;;

  esac

  # :command.command_filter
  action="spec describe"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

    -?* )
      printf "invalid option: %s\n" "$key" >&2
      exit 1
      ;;

    * )
      # :command.parse_requirements_case
      # :command.parse_requirements_case_simple
      if [[ -z ${args[file]+x} ]]; then

        args[file]=$1
        shift
      else
        printf "invalid argument: %s\n" "$key" >&2
        exit 1
      fi

      ;;

    esac
  done

  # :command.required_args_filter
  if [[ -z ${args[file]+x} ]]; then
    printf "missing required argument: FILE\nusage: zen spec describe FILE\n" >&2
    exit 1
  fi

}

# :command.initialize
initialize() {
  version="0.1.0"
  long_usage=''
  set -e

  # src/initialize.sh

}

# :command.run
run() {
  declare -A args=()
  declare -a other_args=()
  declare -a input=()
  normalize_input "$@"
  parse_requirements "${input[@]}"

  if [[ $action == "note" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_note_usage
    else
      zen_note_command
    fi

  elif [[ $action == "note list" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_note_list_usage
    else
      zen_note_list_command
    fi

  elif [[ $action == "note add" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_note_add_usage
    else
      zen_note_add_command
    fi

  elif [[ $action == "todo" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_usage
    else
      zen_todo_command
    fi

  elif [[ $action == "todo list" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_todo_list_usage
    else
      zen_todo_list_command
    fi

  elif [[ $action == "spec" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_spec_usage
    else
      zen_spec_command
    fi

  elif [[ $action == "spec describe" ]]; then
    if [[ ${args[--help]:-} ]]; then
      long_usage=yes
      zen_spec_describe_usage
    else
      zen_spec_describe_command
    fi

  elif [[ $action == "root" ]]; then
    root_command
  fi
}

initialize
run "$@"
